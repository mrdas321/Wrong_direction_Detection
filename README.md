# Wrong_direction_Detection
Using yolov8 pretrained model a wrong direction detection program is developed. That can detect person, car, truck, bike whether it is going trhough correct road or not. Virtual Divider is drawned to detect lane. Video is given as input and it exports the video with anomaly detection. 
